{
  "name": "balatro",
  "version": "1.0.0",
  "description": "A poker roguelike deck-builder, built as an installable web app for iPhone.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "icons": "python3 scripts/gen_icons.py",
    "serve": "python3 -m http.server 8080"
  }
}
